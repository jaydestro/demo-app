apiVersion : apps/v1
kind: Deployment
metadata:
  name: "jayaksdemo-8abb"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "jayaksdemo-8abb"
  template:
    metadata:
      labels:
        app: "jayaksdemo-8abb"
    spec:
      containers:
        - name: "jayaksdemo-8abb"
          image: "jayacrdemo.azurecr.io/jayaksdemo"
          ports:
          - containerPort: 8000